# win-use-master 未来实施方案

> 计划版本：2026-09-21
> 计划范围：从当前 Beta 候选工作区推进到可审计的 Beta 发布，再推进到 v1.0
> 当前状态基线：[`PROJECT_STATUS.md`](PROJECT_STATUS.md)
> 执行原则：里程碑必须通过验收门才能前进；日期是目标，不是绕过安全或测试的理由。

## 当前执行进度（2026-09-21）

- M0-02 无桌面回归已完成：统一入口当前实跑 Contract 层 21/21 通过。
- M1-01 已完成首版：`tests/run-tests.ps1` 支持 Contract/Desktop/Coordinate/Profiles、`-List`、`-DryRun`、`-TestId`、显式 profile、单项截止时间和 0/1/2 聚合。
- M1-02 已完成首版：`win-use-master/test-report-v1` 只保存环境版本、结果、耗时、输出行数和 SHA-256，不嵌入原始测试日志；契约覆盖 dry-run 与真实 parse 执行。
- M1-03 实现完成：CI 已复用统一 parse，增加调度器契约和 UIA timeout，拆分核心/CDP job，并建立 Node 22/24 矩阵；action 固定到完整 SHA且关闭无用缓存。Node 22 结论必须以每个目标提交的远程 Actions 结果为准。
- M2-01～M2-06 已完成首版。M2-06 已覆盖 windows 首次/重复新进程、100/300/1000 合成 UIA 与临时无头 Edge 的只读 CDP inspect；PrintWindow 和 CDP 写性能留待专用桌面/单独授权。M2-05 删除 apply 尚未开放。
- M3-01 已完成首版：`app-profile-catalog-v1` 编码 6 个可重放、3 个只读观察和 1 个未安装占位；能力矩阵由单向脚本确定性生成，并由 Contract/CI 校验分类、测试映射、隐私与 30 天/版本/身份失效策略。
- M3-03 已完成首版：目录绑定的 `profile-test-plan-v1` 把新增档案固化为十阶段检查表；默认退出 2，计划模式零应用、零写命令、零证据、零仓库修改，真实应用逻辑仍由独立 profile 实现。
- M3-04 已完成文字派生首版：UIA/记事本、CDP/WorkBuddy、COM/Excel 三类案例由 `public-cases-v1` 确定性生成并受隐私契约守护；经人工复核的真实截图/GIF 仍为 0，不能把本阶段标成完整完成。
- M4-01～M4-05 已完成首版：威胁模型、跨运行时风险规则、供应链检查、贡献/安全/行为规范、脱敏 Issue Forms、PR 模板、五分钟只读体验、英文 Quick Start 和安装生命周期指南均已落库并受契约守护。
- M5-01 与 M5-04 已完成未发布首版：`release-manifest-v1`、`VERSION`、Changelog、Release Notes 草案、版本/回滚说明和零副作用发布检查已经落库；当前 `0.1.0-beta.1` 仍被缺失的桌面、档案、干净 clone、远程 CI、tag 与校验和证据安全阻塞。
- M0-03～M0-05 尚待专用空闲桌面，因此不能把当前状态标成完整 Beta 发布通过。

## 1. 目标和成功定义

未来工作的主目标不是让工具“能点更多按钮”，而是把现有四层控制面做成可验证、可维护、可扩展、适合公开协作的 Windows 自动化 Skill。

目标分为五类：

1. **发布可信度**：当前本地增强经过完整回归、隐私检查和远程 CI，形成可追踪的 Beta 版本。
2. **核心可靠性**：增强诊断、UIA 定向查询、机器可读输出、能力提示和临时证据治理，不削弱现有安全闸。
3. **真实覆盖率**：从 6 个可重放档案扩到至少 11 个，覆盖 L0、L1、L2、L3 和不同 UI 技术栈。
4. **开源成熟度**：补齐安全披露、贡献流程、版本策略、中英文入口和真实脱敏案例。
5. **稳定发布**：以明确的兼容契约、回归证据和可复现发布流程达到 v1.0，而不是仅修改版本号。

v1.0 的一句话成功标准：在受支持的 Windows 10/11 交互桌面上，工具能对已探测到的能力选择最可靠控制面；所有写入都有安全前置检查和结果验证；未知结果安全停止；新贡献者能在干净环境复现构建、测试和发布结论。

## 2. 计划假设与授权边界

### 2.1 估算假设

- 以 1 名主要维护者计算，另有测试桌面可在指定时段保持空闲。
- PowerShell 7 为必需运行时；CDP 路径支持 Node.js 22 和 24。
- Excel、WPS、WorkBuddy 等商业或本地应用只在已有合法安装时验证，不把安装许可纳入项目工作。
- GitHub 云 CI 只承担无交互桌面的测试；L2、真实截图和真实应用档案在隔离 Windows 测试机完成。
- 工期按工程日估算，不包括等待用户授权、应用下载、账号登录、商店更新和第三方应用变更造成的阻塞。

预计总投入约 **45–69 个工程日**。单人全职建议预留 10–14 周；若只能业余维护，应保持里程碑顺序，延长日期而不是压缩验收。

### 2.2 不随计划自动获得的权限

本文是实施方案，不代表已经授权执行以下动作：

- 安装或卸载应用；
- 重启、显示、关闭用户正在使用的应用；
- 在 QQ、微信、WorkBuddy、剪映等真实账号界面写入内容；
- 删除历史临时目录；
- 创建 GitHub Issue、Release、Tag、提交或推送代码；
- 发送、支付、转账、删除、提交、覆盖保存或执行终端命令等最终动作。

执行到这些节点时必须重新确认目标和范围。风险动作的现有 fail-closed 规则不能为了完成排期而放宽。

## 3. 总体路线与依赖

```text
M0 当前候选版稳定化
        │
        ├──────────────┐
        ▼              ▼
M1 测试与 CI 基础   M4 安全/OSS 基础材料
        │              │
        ▼              │
M2 核心可靠性增强      │
        │              │
        └──────┬───────┘
               ▼
       M3 真实应用扩展
               │
               ▼
       M5 RC 验收与 v1.0
```

关键路径是 `M0 → M1 → M2 → M3 → M5`。M4 的文档和治理工作可以与 M1–M3 并行，但安全模型必须在新控制能力合并前完成审查。

## 4. 推荐 12 周节奏

以下日期以 2026-09-14 为起点，只用于安排，不构成发布承诺。

| 周期 | 日期 | 主里程碑 | 预期产物 |
|---|---|---|---|
| W1 | 09-14～09-20 | M0 候选版稳定化 | 完整本地测试报告、隐私/残留检查、可提交 Beta 候选 |
| W2–W3 | 09-21～10-04 | M1 测试与 CI 基础 | 分层测试入口、Node 22/24 矩阵、统一测试摘要、干净工作区断言 |
| W4–W6 | 10-05～10-25 | M2 核心可靠性 | `doctor`、UIA 限定查询、`--json` 契约、保守能力提示、证据清理入口 |
| W7–W9 | 10-26～11-15 | M3 真实应用扩展 | 至少 11 个应用档案、机器可读档案 schema、三类脱敏案例 |
| W10–W11 | 11-16～11-29 | M4 OSS 与安全收口 | SECURITY/CONTRIBUTING/模板、中英文快速入口、威胁模型、发布策略 |
| W12 | 11-30～12-06 | M5 RC 与 v1.0 决策 | RC 测试证据、干净安装验证、Release Notes、发布或继续保持 Beta 的结论 |

如果 M0 的完整桌面回归未通过，W2 不进入功能开发，优先修复；如果 W12 仍有 P0/P1 验收项未满足，版本继续标为 Beta，不做“带病 v1.0”。

## 5. M0：当前候选版稳定化

目标：证明当前未提交增强可以成为一个可信 Beta 候选，不再混淆“局部测试通过”和“发布通过”。预计 4–6 个工程日。

### M0 工作分解

| ID | 任务 | 产物 | 验收标准 | 依赖 |
|---|---|---|---|---|
| M0-01 | 冻结工作区清单 | 变更文件、生成物、未跟踪文件、当前提交记录 | 每个变更都能归到代码、测试或文档；无来源不明二进制 | 无 |
| M0-02 | 无桌面回归 | 解析、构建、静态、UIA/CDP/窗口状态测试摘要 | 所有无桌面测试退出 0；失败有最小复现 | M0-01 |
| M0-03 | 活跃桌面 smoke | 普通 smoke 与测试环境记录 | 连续两次通过；无敏感窗口；无残留 fixture | M0-02、空闲桌面 |
| M0-04 | 发布级 L2 回归 | `smoke.ps1 -RequireCoordinate` 结果和收据 | 安全闸、焦点/鼠标恢复、遮挡判断均通过；不能因用户活动跳过 | M0-03、专用空闲时段 |
| M0-05 | 专项与真实档案复验 | capture/UIA timeout/CDP/Settings 等结果 | 受本批影响的专项通过；Settings 不复用或关闭用户实例 | M0-02 |
| M0-06 | 隐私与残留审计 | staged diff 扫描、进程/窗口/临时目录报告 | 无账号、设备名、绝对用户路径、token、正文或敏感截图入库；无新增残留 | M0-02～05 |
| M0-07 | Beta 候选发布门 | 可审计提交方案和 CI 结果 | 用户明确要求后才提交/推送；远程 CI 对候选提交为绿色 | M0-06 |

### M0 执行细则

1. 先记录 `git status`、`git diff --stat`、当前提交和测试工具版本，避免测试过程中无法区分原有变化与新产物。
2. 无桌面测试按“解析 → 构建 → 静态契约 → UIA 契约 → 窗口状态 → CDP owner → CDP 收据 → UIA timeout”运行；失败即停止进入桌面测试。
3. 桌面测试前人工确认测试窗口、前台应用和用户空闲状态；测试期间不操作键鼠，不打开含敏感信息的窗口。
4. `smoke` 普通模式通过后再运行 `-RequireCoordinate`，两者结果分开记录；不能把“坐标测试被安全跳过”写成通过。
5. Settings 档案检测到现有设置窗口时返回安全跳过，等待独立时段，不强行关闭。
6. 测试后检查 Edge、fixture、计算器、记事本等测试所有权明确的进程；只清理测试自己创建且能证明所有权的对象。
7. 历史 `%TEMP%` 目录先做 dry-run 清单和 manifest/进程检查，删除动作另行获得授权。

### M0 退出条件

- 所有无桌面测试为绿色；
- 普通 smoke 和 RequireCoordinate 各连续通过两次；
- 本批影响的真实档案或专项测试完成复验；
- 没有敏感信息和新增残留；
- 文档与实际命令帮助一致；
- 远程 CI 通过，或者明确标记“尚未授权推送”，不虚报远程结论。

## 6. M1：测试与 CI 基础设施

目标：把“应该跑哪些测试、在哪里跑、输出怎样保存”变成稳定契约。预计 7–10 个工程日。

### M1-01：分层测试入口

新增一个统一测试调度脚本，例如 `tests/run-tests.ps1`，但不替代单项测试：

- `-Tier Contract`：解析、构建、静态、UIA/CDP/窗口状态等无桌面测试；
- `-Tier Desktop`：capture、fixture、普通 smoke；
- `-Tier Coordinate`：只运行明确要求真实 L2 的回归；
- `-Tier Profiles`：按已安装应用显式选择真实档案；
- `-List`/`-DryRun`：只列计划，不启动应用；
- 默认不运行真实账号应用，不自动安装依赖。

验收标准：单项退出码被完整保留；退出 2 显示为“安全拒绝/未知”，不能聚合成通过；调度器失败后仍执行所有权明确的清理，并给出失败项列表。

### M1-02：结构化测试报告

为测试摘要定义 `win-use-master/test-report-v1`：

- Git commit、工作区是否干净、Windows/PowerShell/Node 版本；
- 测试 ID、开始/结束时间、耗时、退出码和结果类别；
- 是否需要交互桌面、是否实际执行坐标输入；
- 创建和清理的进程/目录数量；
- 收据路径只使用相对或临时标识，不记录用户名和敏感标题。

验收标准：人类可读摘要和 JSON 来自同一数据源；测试失败时也能生成不含正文的报告。

### M1-03：CI 运行时矩阵

- Node.js 覆盖 22 和 24，确保文档支持下限真实受测。
- PowerShell 固定为 7.x，并在日志中输出精确版本。
- 将纯解析/静态检查与需要 Edge 的集成测试分开，便于定位失败。
- 保持 `permissions: contents: read` 和并发取消策略。
- 对 Actions 版本采用稳定、真实存在的主版本，并定期更新；升级必须经 CI 验证。

验收标准：两个 Node 版本均通过 CDP 解析和关键集成测试；CI 失败能直接定位到单一测试层。

### M1-04：交互桌面测试策略

GitHub 托管 runner 不提供可信用户交互桌面，因此：

- 不把 L2、截图/真实 UIA 或计算器测试塞进云 CI 后宣称通过；
- 可选配置专用自托管 Windows 测试机，但默认不接入真实账号和个人桌面；
- 自托管任务只接受受控 fixture 和内置测试应用，执行前锁定测试会话，完成后恢复快照或清理；
- 若没有自托管机，维持人工发布门并保存脱敏测试摘要。

### M1-05：失败分类与防抖

统一测试失败类别：`assertion-failed`、`safe-refusal`、`effect-unknown`、`environment-missing`、`provider-timeout`、`cleanup-failed`。只允许对纯读取、幂等且已证明无副作用的准备步骤进行有限重试；写动作、Enter/保存/关闭等不得自动重放。

### M1-06：生成物与清理契约

- 所有测试临时目录必须带项目专属前缀、manifest、owner PID/启动时间和创建时间。
- 清理前验证路径位于系统临时目录下的项目命名空间，验证无活跃 owner，再删除精确目录。
- CI 最后使用 `git status --short --untracked-files=all` 保证仓库没有生成物。
- 清理失败是测试失败，不能只写 warning。

## 7. M2：核心可靠性增强

目标：提高诊断和通用读取能力，让调用者更少依赖猜测，同时保持安全默认值。预计 12–18 个工程日。

### M2-01：只读 `doctor` 命令

状态：**已实现，待本批提交/远程 CI 验证。** 生产入口在 helper 加载前分发，五类 fixture 已覆盖干净环境、缺 Node、helper 过期、安全桌面和历史残留；`doctor-report-v1`、文本/摘要、隐私和零副作用由 Contract 层守护。为坚持零写入，临时目录只报告是否存在，实际写权限为 `unknown/not-probed`，不会用探测文件换取“可写”结论。

新增 `win.ps1 doctor [--json] [--summary]`，默认只读，检查：

- PowerShell 版本和执行策略是否满足运行条件；
- C# helper 是否存在、是否需要重编译、编译器是否可用；
- Node.js 是否满足 CDP 支持范围；
- Edge/Chromium 可执行文件是否可发现；
- 风险规则 schema 和收据 schema 是否可读；
- 当前是否为交互输入桌面、是否锁屏、当前完整性级别；
- 临时目录是否可写、是否发现项目所有权明确的历史残留；
- 哪些测试可以在当前环境安全运行。

约束：`doctor` 不启动应用、不编译、不删除、不修复，只输出建议。缺少可选 CDP 依赖不能让纯 UIA/截图能力整体报废。

验收标准：在干净环境、缺 Node、helper 过期、锁屏/非交互桌面和存在历史残留的 fixture 中都有确定输出；`--summary` 不泄露绝对用户路径。

### M2-02：UIA 限定查询与分页

状态：**已实现，待本批提交/远程 CI 与真实 UIA 桌面复验。** `uiaread` 已支持精确/前缀 ID、ControlType、精确/前缀 Name、唯一子树、显式 limit 和 `uia-continuation-v1`。无桌面 provider fixture 已覆盖 300 项后查询、组合过滤、正文延迟读取、分页推进、token 脱敏以及树/窗口变化失效；现有位置过滤词和单独 `--id` 行为保持兼容。

已实现范围（旧位置模糊过滤仍按兼容约定受 300 项上限影响）：

- 按 `AutomationId`、`ControlType`、Name 的精确或显式前缀条件组合；
- `--within-id`/限定子树，避免扫描整个窗口；
- 明确的 `--limit` 和 continuation 信息；
- 先匹配元数据，再按用户请求读取 Value；
- 元素树发生变化时 continuation 失效并返回 2，不使用旧引用继续动作。

兼容策略：保留现有 `uia`/`uiaread` 文本输出和短引用；新查询能力通过附加参数启用，不改变旧命令默认含义。

验收标准：目标位于第 300 项之后仍可通过限定条件找到；多个同 ID、相似 ID、树变化、密码控件和 provider 超时均有契约测试。

### M2-03：统一机器可读输出

状态：**已实现，待本批提交/远程 CI。** `doctor`、`probe`、`windows`、`frontmost`、`idle`、`uia`、`uiaread`、`restore/minimize` 状态结果，以及 CDP `list/inspect` 已有版本化 schema。默认文本保持兼容；摘要隐私、查询/选择器不回显、URL query/fragment 清理、unknown/null、写状态 partial 和单文档 stdout 由无桌面/无头契约守护。

为主要只读命令增加可选 `--json`，不改变默认文本输出。优先覆盖：

- `windows`、`frontmost`、`idle`（已完成）；
- `probe`、`doctor`（已完成）；
- `uia`、`uiaread`（已完成）；
- `restore`/`minimize` 的状态结果（已完成）；
- CDP 目标和 inspect 摘要（已完成；其它 CDP 读取仍保留人读输出）。

每类输出必须有版本化 schema、稳定字段和敏感字段策略。终端摘要、JSON 和落盘 receipt 应区分用途，避免为了结构化而把正文复制三份。

验收标准：schema 正向/反向样例有测试；未知状态保留为 `unknown`，不能用 `false` 或空字符串掩盖。

### M2-04：保守的应用能力提示

状态：**已实现，待本批提交/远程 CI。** `capability-cache-v1` 只由只读 probe 和独立 `cache show/record/clear` 入口加载；记录动作必须显式给出 full `probe-report-v1`，默认文件 30 天失效且版本变化立即失效。字段白名单不包含标题、正文、路径、PID/端口或 L2/授权状态，summary 不列 entry，`show` 零写入，`--no-cache`/环境变量可完全禁用读取。

增加本地 `capability-cache-v1`，只保存不敏感的能力观察，例如产品名、版本、exe 名、窗口类和“UIA provider 曾超时/发现 COM/CDP”的时间戳。

约束：

- 不保存窗口标题、UIA Name/Value、DOM 文本、账号、完整命令行和文档路径；
- 默认 30 天失效，应用版本变化立即失效；
- 缓存只用于调整探测顺序和提示，不能替代实时 PID owner、端口、权限、桌面、前台或风险校验；
- 提供只读查看和显式清除能力，清除前精确验证缓存文件路径。

验收标准：伪造缓存不能授权任何写入；版本变化和过期时间测试通过；禁用缓存时行为与当前版本一致。

首版验收结果：`tests/capability-cache-contract.ps1` 已验证字段规范化、30 天/未来时间、版本变化、摘要隐私、只读查看文件哈希与时间戳不变、禁用时零修改、精确删除边界，以及伪造授权字段不能绕过 CDP session。真实写路径没有导入缓存模块。

### M2-05：证据和临时数据治理

状态：**只读阶段已实现，删除阶段未开放。** `cleanup [--dry-run] [--json] [--summary]` 固定扫描系统临时根的项目直接子目录，验证唯一 `temp-artifact-v1` manifest、artifact ID、30 天内生命周期、到期状态、owner PID/启动时间、reparse point 和 10,000 项枚举上限。报告不含绝对路径或 manifest 未知字段，所有 side-effect 计数为 0；`--apply` 在扫描前退出 2。

规划 `cleanup --dry-run` 与显式 `cleanup --apply`：

- 默认永远 dry-run；
- 只处理带有效项目 manifest 的临时对象；
- 检查 owner 进程、启动时间、绝对目标是否位于预期临时根；
- 不接受通配符、相对路径、环境变量展开后为空或工作区根目录；
- 输出将删除内容、大小、创建时间和拒绝原因；
- 删除失败保留列表，不扩大路径范围重试。

该命令属于破坏性维护动作，真正执行仍需用户明确要求。`tests/cleanup-contract.ps1` 已用逐文件哈希/长度/时间戳证明 dry-run 与 apply 拒绝零修改；开放 `--apply` 前仍需二次扫描、防 TOCTOU 绑定、逐项结果和部分失败契约。

### M2-06：性能基线

状态：**只读首版已实现，桌面/写入第二阶段待办。** `benchmark [--quick] [--no-cdp] [--json] [--summary]` 生成 `performance-report-v1`；标准档对重复 windows、三档 UIA 与 CDP inspect 各采 5 个样本。UIA 直接执行生产查询函数但只接合成 provider，CDP 只使用本次自启、可精确回收的临时无头 Edge；报告只有聚合值与副作用计数。首次本机结果、口径和局限见 `references/性能基线.md`。

先测量再设目标，记录：

- `windows` 冷/热启动耗时；
- 100、300、1000 元素 UIA 树的查询耗时和超时率；
- PrintWindow 成功率、超时率和 sibling recovery 次数；
- CDP inspect/insert/press 的 p50/p95 时延；
- 每轮测试创建/回收进程和临时目录数量。

性能改动不得通过延长危险写入超时、取消 UIA 隔离或跳过安全校验获得。

首版验收结果：`tests/benchmark-contract.ps1` 已验证 nearest-rank p50/p95、quick/no-CDP 的 windows/UIA 结果、摘要隐私、仓库逐文件零修改、参数在测量前拒绝，以及生产 UIA/CDP 超时常量保持不变。标准档另实跑只读 CDP inspect 并确认临时 profile/owner 进程清零。PrintWindow 与 `insert/press` 没有在本轮执行，不能写成已覆盖。

## 8. M3：真实应用覆盖扩展

目标：把应用档案从当前 6 个可重放 + 3 个只读观察，扩展到至少 11 个有明确结论的样本。预计 12–20 个工程日，第三方应用版本变化可能增加等待时间。

### M3-01：机器可读应用档案

为每个档案增加独立的元数据文件或受验证的 frontmatter，建议字段：

- `appId`、产品名、版本、发布日期/验证日期；
- 进程身份、窗口类、架构和宿主关系；
- 已发现的 L0/L1/L2/L3 能力；
- 已验证任务、可逆策略、停手线；
- 对应测试脚本、最后结果和证据是否脱敏；
- 易腐假设和下次版本复验条件。

由脚本从元数据生成 README/档案能力矩阵，避免手工统计与测试文件漂移。生成脚本必须是单向、确定性的，不覆盖人工经验正文。

首版验收结果：`config/app-profiles.json` 已包含 10 条目录记录，明确区分 9 个实测样本与 Blender 未安装占位；`scripts/generate-app-matrix.ps1` 只生成 `references/应用能力矩阵.generated.md`，不会覆盖人工 `app档案.md`。`tests/app-profile-catalog-contract.ps1` 校验必需字段、能力枚举、6/3/1 分类、真实测试文件/调度器映射、脱敏派生证据、复验条件和生成漂移。M3-01 已完成首版，M3 的样本扩展与公开脱敏案例仍未完成。

### M3-02：新增样本优先级

| 顺序 | 样本类型 | 目标 | 选择理由 | 安全要求 |
|---|---|---|---|---|
| 1 | Blender | 验证 bpy/CLI 的 L0 创作路径 | 与 Mac 设计理念直接对应，避免 UI 坐标 | 只创建临时工程，保存到测试目录 |
| 2 | 第二个 Chromium/CEF 应用 | 验证 CDP owner/session/编辑器差异 | 防止能力只适配 WorkBuddy | 用户授权启动调试实例；不发送内容 |
| 3 | 非 Office 原生 Windows 应用 | 验证 UIA/L3 通用性 | 扩展 WinUI/Win32 样本 | 选择本地、可逆、无账号任务 |
| 4 | QQ | 探索 UIA/CDP 写路径 | 当前只有 UIA 只读结论 | 用户指定草稿式可逆目标；禁止发送 |
| 5 | 微信 | 验证“无语义层时安全停手”或受控 L2 | Qt5 空树是重要反例 | 只在隔离测试账号/草稿区；禁止发送 |
| 6 | 剪映 | 验证主编辑器和 CEF 线索 | Qt6 provider 超时已观察 | 重启、调试端口和更新弹窗都需单独授权 |

若某应用无法形成可靠写路径，也可以作为“已证明不可安全自动化”的只读档案；不能为了达到数量把盲点坐标包装成成功案例。

### M3-03：真实档案测试模板

每个新档案必须按同一顺序：

1. 记录版本和安装来源；
2. `probe` 只读发现，不启动未知服务；
3. 核对 PID、exe、宿主、窗口类和完整性；
4. 选择最低控制层；
5. 建立 before 证据；
6. 只执行可逆写入或隔离对象操作；
7. 通过独立通道读回，不相信动作返回值；
8. 撤回/关闭测试对象，检查无残留；
9. 删除含正文的临时证据；
10. 把共性问题优先变成代码或测试，易腐细节才写入 app 档案。

首版验收结果：`tests/profile-test-template.ps1` 只接受目录中的唯一 appId，未显式 `-Plan` 时退出 2；JSON 计划使用 `win-use-master/profile-test-plan-v1`，声明精确目标、最低可靠层、独立读回、回滚/隔离、finally 清理和拒绝/unknown 退出码。`tests/profile-template-contract.ps1` 端到端验证默认拒绝、未知 app、冲突参数、十阶段顺序、摘要隐私、无桌面执行 primitive 和仓库逐文件零修改。具体实现要求见 `references/应用档案测试模板.md`。M3-03 已完成首版，但新增真实应用仍必须逐个实测。

### M3-04：脱敏案例素材

至少制作三类案例：

- UIA：计算器或记事本的语义读取/可逆写；
- CDP：受控编辑器的插入和撤回，发送按钮保持未触发；
- COM：临时 Excel/WPS 文件的写入、保存和脱离宿主验证。

每个案例展示“探测 → 选择控制面 → 动作 → 独立验证 → 清理”，并对标题、用户名、设备名、路径、正文和通知区域做脱敏。原始证据不入库，只提交审查后的派生素材。

文字派生首版结果：`config/public-cases.json` 编码记事本 UIA、WorkBuddy CDP 和 Excel COM 三个已验证案例，`scripts/generate-public-cases.ps1` 单向生成 `references/脱敏真实案例.generated.md`。契约从真实 profile 测试交叉核对输入长度、步骤、公式和合计，禁止正文、绝对路径、动态标识、URL/邮箱和 fixture 视觉替代。当前 `visual.status` 均为 `not-included-pending-reviewed-source`，因此 M3-04 只完成文字素材和发布边界，截图/GIF 仍待真实桌面与人工脱敏复核。

### M3 验收门

- 至少 11 个应用有版本化实测结论；
- 至少 8 个形成可重放测试或严格步骤，其余可以是明确的安全边界档案；
- L0、L1、L2、L3 均有真实样本，但 L2 不以扩大风险换覆盖率；
- 新应用版本变化能触发档案过期提示；
- 三类公开案例通过隐私复核。

## 9. M4：安全、隐私和开源治理

目标：让外部用户知道工具能做什么、不会做什么、如何报告问题，也让贡献者不破坏安全不变量。预计 6–9 个工程日。

### M4-01：威胁模型

状态：**已实现首版。** `THREAT_MODEL.md` 以稳定 ID 覆盖 UI 提示注入、CDP 身份、桌面/UIPI、坐标漂移、写后 unknown、证据泄露、清理越界、供应链和恶意贡献；每项均列出控制、测试、残余风险与停手线，并由 `governance-contract.ps1` 防止映射漂移。

新增 `THREAT_MODEL.md`，至少覆盖：

- 恶意窗口标题、DOM/UIA 文本对代理的提示注入；
- CDP 端口劫持、PID 复用、target 混淆和 session 过期；
- UIPI/UAC、锁屏、其他桌面和前台劫持；
- 坐标遮挡、窗口移动、DPI/缩放和旧截图引用；
- 写超时后的效果未知与重复提交风险；
- 收据、截图、UIA map、命令行和临时目录的信息泄露；
- 测试清理误删和路径扩张风险。

每个威胁给出资产、攻击面、现有控制、残余风险和对应测试；不把操作系统硬边界写成“已解决”。

### M4-02：风险规则强化

状态：**已实现首版。** JSON 明确声明 NFKC、camelCase、分隔符、空白折叠和 trim 顺序；UIA/L2 与 CDP 分别使用共享 PowerShell/Node 解释器。契约以 12 个中英文/全角/camelCase/下划线正例、15 个词边界负例覆盖全部 8 条规则，并确认缺规范化、重复 ID 和非法 flags 在两个运行时失败关闭。现有 CDP 真实动作拒绝与 UIA worker 超时链已复跑。

- 扩展中英文最终动作正反例、Unicode NFKC、camelCase、下划线和近义词测试；
- UIA、CDP、L2 对同一规则版本保持一致；
- 风险规则变更必须同时给出误报和漏报样例；
- `--force` 仍不能绕过最终动作；若未来需要授权机制，应设计独立、一次性、目标绑定的确认，而不是全局开关。

### M4-03：敏感信息和供应链检查

状态：**已实现无外部依赖的首版。** `SUPPLY_CHAIN.md` 记录 Windows/PowerShell/Node/Edge 运行时、零包管理依赖、MIT 许可证和两项固定 SHA 的 GitHub Action；`repository-hygiene-contract.ps1` 扫描高置信凭据、未审查媒体/生成二进制、包 manifest、workflow Action 与包安装命令。它不上传文件或匹配内容，也不替代历史提交、runner 镜像和 Action 上游的人工审查。

- CI 增加仓库级 secret/credential 扫描，规则排除合成测试值但不排除整个证据目录；
- 检查 PowerShell/Node 依赖和许可证；当前无包管理依赖也应记录事实；
- GitHub Actions 固定到可信版本或 commit，并定期审查更新；
- CI 不上传真实截图、UIA map、DOM 快照和用户目录日志。

### M4-04：开源治理文件

状态：**已实现首版。** `CONTRIBUTING.md`、`SECURITY.md`、`CODE_OF_CONDUCT.md`、Bug/app-profile/security-contact Issue Forms 和 PR 模板已完成。由于仓库当前没有公开的安全邮箱且私密漏洞报告尚未启用，备用公开表单没有自由文本字段，只用于建立私密渠道，不能承载漏洞细节。

建议新增：

- `CONTRIBUTING.md`：开发环境、分层设计、安全不变量、测试分层、提交要求；
- `SECURITY.md`：支持版本、私下报告渠道、不要提交真实证据；
- `CODE_OF_CONDUCT.md`；
- `.github/ISSUE_TEMPLATE/bug.yml`：版本、控制层、退出码、已脱敏复现，禁止直接粘贴敏感正文；
- `.github/ISSUE_TEMPLATE/app-profile.yml`：应用版本、探测结果、可逆任务、停手线；
- `.github/pull_request_template.md`：风险、测试、隐私、兼容性清单。

### M4-05：对外文档

状态：**已实现首版。** README 已增加只运行 doctor/probe/windows 的五分钟只读体验、英文 Requirements/Quick Start/Beta 边界和退出码 2 说明；`references/安装升级与卸载.md` 区分 OpenAI Skill 目录规范与第三方 skills CLI，覆盖 CLI/Git 升级、精确卸载、缓存/CDP session/helper/版本检查/evidence 清单和非递归清理。当前没有独立 UI 分发需求，因此按 skill-creator 约束不创建会复制入口内容的 `agents/openai.yaml`。

- README 保持中文主入口，同时增加精简英文 Quick Start 或英文 README；
- 增加“5 分钟只读体验”，默认只运行 `probe/windows/doctor`；
- 增加完整的卸载、升级和本地数据清理说明；
- 明确 Beta 支持范围、退出码 2、桌面要求和不支持场景；
- 评估 `agents/openai.yaml` 的展示元数据和默认提示，但只有在确有 UI 分发需求时增加，不复制 `SKILL.md` 内容。

## 10. M5：RC、版本策略和 v1.0

目标：对完整候选版本做独立验收，满足标准才发布 v1.0。预计 4–6 个工程日。

### M5-01：版本和兼容策略

采用语义化版本：

- Patch：修复、文档和不改变契约的安全加固；
- Minor：向后兼容的新命令、参数或 schema；
- Major：默认行为、命令语义、收据 schema 或安全授权模型的不兼容变化。

现有 `receipt-v1`、`uia-map-v1`、`action-receipt-v1` 在 v1.0 后视为公开契约；新增字段应保持读取兼容，删除/改义需要 major 版本。

首版验收结果：`config/release.json` 是版本、通道、发布状态和发布门的真相源，`VERSION` 为单行投影；当前版本为 `0.1.0-beta.1`、状态 `unreleased`。`release-contract.ps1` 守住 SemVer、文档同步、既有 `*-vN` 含义和退出码兼容规则。

### M5-02：Release Candidate

从干净 clone 验证：

1. Skill 可被发现并安装；
2. helper 可从源码构建；
3. 无桌面测试全绿；
4. 活跃桌面 smoke 和坐标回归各连续两次通过；
5. 真实应用矩阵达到要求；
6. 生成物、临时目录和测试进程全部回收；
7. 文档链接、命令帮助、版本和 Release Notes 一致；
8. secret/隐私检查无高危发现；
9. 远程 CI 对 RC commit 为绿色。

### M5-03：人工抽查

发布者至少人工复核四条完整证据链：

- 一个 L0 COM 或 CLI 案例；
- 一个 L1 UIA 案例；
- 一个 L0 CDP 案例；
- 一个 L2 坐标案例。

抽查重点是动作前身份、动作内容、独立验证和清理，而不是只看最终截图。

### M5-04：发布与回滚

- 生成 Release Notes：新增、修复、安全变化、已知限制、升级说明；
- 创建签名 tag 或受保护 tag，发布源码校验和；
- 验证安装命令指向预期版本；
- 保留上一稳定 Beta 的安装方式和回滚说明；
- 发布后 72 小时只处理 P0/P1 回归问题，不立即合并新的大功能。

如果任何强制验收项失败，输出阻塞清单并继续标记 Beta；不因日期、宣传或申请需要跳过门槛。

首版验收结果：`scripts/release-check.ps1` 只读汇总 manifest、文档与 Git 工作区，不创建或修改 commit、tag、Release 和文件；有任何必需门未通过时退出 2。当前没有 tag、published commit 或 previous stable，故发布和真实回滚验证仍未完成。

## 11. 质量门与 Definition of Done

### 11.1 单项变更完成标准

每个代码任务只有同时满足以下条件才算完成：

- 功能行为与非目标写清；
- 安全拒绝和退出码语义不被削弱；
- 有可观察行为测试，不只是匹配文档字符串；
- PowerShell/JavaScript 解析和相关专项测试通过；
- 新临时对象有所有权、截止时间和清理路径；
- 收据不包含原始正文、token、绝对用户路径或多余标题；
- README/SKILL/reference/help 只更新真正受影响的入口，避免重复文档；
- `git diff --check` 和 Skill validator 通过；
- 未经用户明确要求，不提交、不推送、不创建外部对象。

### 11.2 Pull Request 门

| 门 | 必须证明 |
|---|---|
| 设计 | 为什么选择 L0/L1/L2/L3，为什么不能用更低风险层 |
| 安全 | 新动作的身份、权限、目标、最终动作和超时策略 |
| 测试 | 无桌面测试、需要桌面的测试、实际执行/跳过项 |
| 隐私 | 日志、收据、截图、map、临时目录的字段和生命周期 |
| 兼容 | 旧命令、schema、退出码和已有应用档案是否变化 |
| 清理 | 进程、窗口、文件和调试端口如何关闭或保留 |

### 11.3 发布门

- P0 为零；
- 远程 CI 绿色；
- 桌面回归不是 skip；
- 真实应用档案达到当前里程碑数量；
- secret/隐私检查通过；
- 干净 clone 安装和构建通过；
- 维护文档和 Release Notes 完成；
- 失败时有明确回滚路径。

## 12. 指标与项目看板

### 12.1 建议跟踪指标

| 类别 | 指标 | Beta 候选目标 | v1.0 目标 |
|---|---|---|---|
| 契约测试 | 无桌面测试通过率 | 100% | 100% |
| 桌面测试 | 普通/坐标 smoke | 各连续 2 次 | 发布前各连续 2 次 |
| 清理 | 测试拥有的残留进程/目录 | 0 新增 | 0 |
| 应用覆盖 | 可重放档案 | 6 个全部复验 | ≥8 个可重放、≥11 个总样本 |
| 控制层 | 真实案例 | L0/L1/L3，L2 受控 | L0/L1/L2/L3 均有脱敏案例 |
| 安全 | 高危 secret/隐私发现 | 0 | 0 |
| 文档 | 相对链接与命令帮助漂移 | 0 | 0 |
| 兼容 | 未说明的 schema/退出码破坏 | 0 | 0 |

性能指标在 M2 建立基线后再定阈值。建议以“相同 fixture 的 p95 不回退超过 20%”作为初始报警线，而不是未经测量就承诺绝对毫秒数。

### 12.2 看板结构

建议使用以下列：

```text
Backlog → Ready → In Progress → Needs Desktop → Needs User Authorization
        → Review → Verified Locally → Verified Remotely → Done
```

`Needs Desktop` 和 `Needs User Authorization` 必须独立，避免把“等待安全条件”误认为普通开发阻塞。每个任务至少记录：任务 ID、风险层、依赖、测试层、是否涉及真实账号、验收证据和清理结果。

## 13. 人员与并行方式

### 单人维护

严格按 M0 → M1 → M2 → M3 → M4 → M5 推进。M4 的纯文档任务可在等待空闲桌面或应用安装时穿插，但不要同时修改同一安全关键脚本的多个主题。

### 两人协作

- A：Windows 核心、UIA/CDP、测试基础设施；
- B：真实应用档案、文档、隐私和 OSS 治理；
- 两人交叉审查风险规则、收据字段和发布报告；
- 真实桌面测试由未编写该变更的人复核一次，降低确认偏差。

### 三人及以上

可将测试/发布、核心控制面、应用档案/文档拆成三条工作流，但共享同一个 schema、安全不变量和发布门。不得为了并行复制 `win.ps1`、CDP 风险规则或应用能力真相源。

## 14. 风险登记表

| 风险 | 概率/影响 | 预警信号 | 缓解措施 | 停止条件 |
|---|---|---|---|---|
| 用户活动导致桌面测试抖动 | 高/中 | idle 闸拒绝、前台变化 | 专用空闲时段；不放宽闸门 | 无法保证独占测试桌面 |
| 第三方应用升级导致档案失效 | 高/中 | 版本/窗口类/AutomationId 变化 | 版本化档案、过期提示、重新 probe | 身份或写路径无法独立验证 |
| CI 被误当成桌面能力证明 | 中/高 | 云日志只有 headless 测试 | 测试分层、发布报告明确 skip | 没有真实桌面证据 |
| CDP 端口/target 混淆 | 低/高 | owner、启动时间或 target 数变化 | 每次重验 session，多个 target 拒绝自动选 | 任一身份字段未知 |
| UIA provider 卡死 | 中/高 | worker 超时 | 进程隔离、硬截止、能力提示 | 写超时后一律 unknown |
| 证据泄露账号或正文 | 中/高 | UIA map/截图/标题出现敏感字段 | summary、字段脱敏、发布前扫描 | 无法证明素材已脱敏 |
| 清理误删 | 低/极高 | 路径不在临时根、manifest 缺失 | dry-run、精确路径、owner 检查 | 任一路径验证失败 |
| 为覆盖率降低安全标准 | 中/极高 | 使用盲点坐标、force 绕过 | 把“不可安全自动化”计为有效结论 | 最终动作或目标不明确 |
| 文档与代码漂移 | 中/中 | 帮助/README/schema 不一致 | 生成能力矩阵、静态契约、发布审查 | 无法确定真实行为 |
| 范围膨胀为“支持所有 app” | 高/高 | 不断加入单 app 特例 | 控制层优先、样本准入、明确非目标 | 只能靠应用版本硬编码 |

## 15. 第一个两周迭代的具体安排

### 第 1–2 天：基线与无桌面测试

- 冻结当前变更清单；
- 记录 PowerShell、Node、Windows、Edge 和应用版本；
- 运行解析、构建、静态发布契约、Skill validator；
- 运行 UIA read、window state、CDP ownership、CDP receipt、UIA timeout；
- 对失败建立最小复现，不启动真实桌面测试。

交付：`M0-test-report` 初稿、失败清单、无桌面测试证据。

### 第 3–4 天：桌面和专项回归

- 预约用户不操作的测试时段；
- 普通 smoke 连续两次；
- RequireCoordinate 连续两次；
- capture recovery、Settings、计算器/记事本按需要复验；
- 记录执行和 skip，不能用历史结果填补本次缺口。

交付：桌面测试报告、动作收据摘要、残留清单。

### 第 5 天：隐私与候选版收口

- 检查 diff、未跟踪文件、生成物、截图/map/receipt；
- 核对 0/1/2 退出语义和新增帮助；
- 更新状态和交接文档；
- 给出建议提交拆分；
- 只有用户明确要求才 commit/push，并等待远程 CI。

交付：Beta 候选决策。若任何桌面测试未真实执行，结论必须是“未通过发布门”。

### 第 2 周：M1 最小闭环

- 设计并实现分层测试调度器；
- 定义 `test-report-v1`；
- CI 增加 Node 22/24 和清晰测试分组；
- 为 cleanup 失败、skip 和退出码 2 增加聚合测试；
- 从干净 clone 验证无桌面层。

交付：一个外部贡献者能按单一入口复现的无桌面测试闭环。

## 16. 建议的 Issue 拆分

为避免大提交难以审查，建议至少拆成以下 Issue：

1. `REL-001` 当前候选版发布级回归与隐私审计；
2. `TEST-001` 分层测试调度器和 `test-report-v1`；
3. `CI-001` Node 22/24 矩阵与 CI 分组；
4. `CORE-001` 只读 `doctor`；
5. `UIA-001` 限定子树、条件组合与分页；
6. `SCHEMA-001` 主要只读命令 `--json`；
7. `CACHE-001` 不参与授权的能力提示缓存；
8. `CLEAN-001` 临时证据 dry-run 清理；
9. `APP-001` 应用档案机器可读 schema 和矩阵生成；
10. `APP-002` Blender L0 档案；
11. `APP-003` 第二 Chromium/CEF 档案；
12. `APP-004` 非 Office 原生 Windows 档案；
13. `SEC-001` 威胁模型与风险规则扩展；
14. `OSS-001` 贡献、安全和模板文件；
15. `DOC-001` 中英文快速开始与脱敏真实案例；
16. `REL-002` v1.0 RC、版本策略、Release Notes 和回滚验证。

每个 Issue 应包含：背景、范围、明确非目标、安全影响、实现落点、测试层、证据字段、清理方式、依赖和验收标准。

## 17. 最终执行建议

最合理的下一步是完成 M0 尚未闭合的 Desktop/Coordinate 发布门，并推进 M3 的真实应用档案。M1、M2、M4 首版与 M5 的版本/回滚契约已经形成，后续应把新增能力与真实应用档案都接入现有分层测试、威胁模型、隐私门和 `release-manifest-v1`，再在干净 clone 与目标 SHA 上验证候选。

执行过程中始终保留三条底线：无法确定目标就退出 2，无法确定写入效果就停止，无法证明证据已脱敏就不公开。
