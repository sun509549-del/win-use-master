# 0.1.0-beta.1 Release Notes（草案，未发布）

> 状态：`unreleased`。当前没有 release tag、发布日期、published commit 或上一稳定版本。本文件是候选说明，不是发布公告。

## 候选内容

- 以 L0 应用接口/COM/CDP、L1 UIA、L2 前台坐标和 L3 截图组成 Windows 四层控制面。
- 所有写路径要求精确目标、安全预检、独立验证和可逆/隔离策略；退出码 `2` 表示安全拒绝或 unknown，绝不能当成功。
- 六个可重放真实应用档案、三个只读观察档案，以及不计入样本的 Blender 未安装占位。
- 无桌面 Contract、Windows CI、威胁模型、供应链与仓库卫生、机器档案、公开派生案例和维护文档。

## 当前验证

- 本地 Contract：`21/21` 已于 2026-09-21 通过；这只证明当前工作区的无桌面层，不替代干净 clone、目标 SHA 远程 CI 或真实桌面回归。
- UIA/CDP/COM 三类公开文字案例通过来源一致性和隐私契约；经人工复核的真实截图/GIF 为 `0`。
- 最新公开 `main` 的历史 CI 结果不能替代本候选 commit 的远程 CI。

## 发布阻塞项

- Desktop 与 Coordinate 发布门尚未在专用空闲桌面连续通过两次。
- 当前只有 6 个可重放、9 个实测样本，未达到 v1.0 的 8/11 目标。
- 干净 clone、候选 SHA 的远程 CI、签名或受保护 tag、源码校验和均未完成。
- 当前没有上一稳定发布，无法声称已经验证从稳定版本回滚。

## 已知限制

- 只支持 Windows 10/11 活动交互桌面；不承诺锁屏、服务账户、断开的 RDP 或安全桌面。
- L2 必须短暂借前台，Foreground Lock、UIPI、DPI、遮挡和窗口移动都可能导致安全拒绝。
- QQ、微信和剪映仍只有只读结论；Blender 尚未安装。视觉案例仍待真实来源与人工脱敏复核。
- `cleanup --apply` 未开放；能力缓存不参与写授权。

## 升级与回滚

升级、卸载和本地数据边界见 [`references/安装升级与卸载.md`](references/安装升级与卸载.md)。版本兼容规则见 [`references/版本与发布.md`](references/版本与发布.md)，安全回滚步骤见 [`references/回滚与恢复.md`](references/回滚与恢复.md)。在正式 tag 出现前，不要使用本草案中的版本号作为可安装发布承诺。
