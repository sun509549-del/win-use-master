# 安全政策

## 支持范围

项目尚未发布稳定版本或维护分支；`config/release.json` 中的 `0.1.0-beta.1` 仍是未发布候选。当前只对公开仓库最新 `main` 提供尽力而为的安全修复；旧 commit、fork、修改版和未合并分支不保证修复。安全修复发布前仍须通过适用契约，不能以紧急为由削弱失败关闭。

## 私密报告漏洞

请优先打开仓库的 **Security** 页面，使用 **Report a vulnerability**（私密漏洞报告）提交。如果该按钮当前不可用，请使用仓库的 **Security/private contact request** Issue 表单：它只允许选择联络类型和确认复选框。公开请求中不要描述漏洞、受影响目标、复现步骤或证据；等待维护者建立私密沟通渠道后再提供详情。

绝不要在公开 Issue、Discussion、Pull Request、CI 日志或附件中提交：

- 利用代码、精确复现步骤或尚未修复的绕过方法；
- token、cookie、账号、私人正文、完整命令行或用户绝对路径；
- 未脱敏截图、UIA map、DOM 快照、动作收据或临时目录；
- 第三方用户、组织或设备的身份信息。

私密报告建议包括受影响 commit/版本、Windows/PowerShell/Node 版本、控制层、前置条件、最小复现、实际与预期结果、影响评估，以及已经脱敏的证据。请说明测试是否可能写入真实应用或外部系统；不要为了报告而执行发送、支付、删除或其他不可逆动作。

维护者会尽力确认报告、评估严重性并协调修复，但当前项目不承诺固定响应或修复 SLA。修复公开前，请给维护者合理的调查时间。

## 适用问题

欢迎私密报告：风险规则或 `--force` 绕过、CDP owner/session/target 混淆、UIPI/桌面/前台/遮挡闸绕过、写超时后的危险自动重放、敏感信息进入日志或收据、清理越界、CI/依赖供应链问题，以及可造成非预期真实副作用的其他缺陷。

普通崩溃、文档问题、已脱敏的兼容性缺陷可使用公开 Bug 表单；不确定是否敏感时按安全问题处理。项目的资产、边界和控制见 [`THREAT_MODEL.md`](THREAT_MODEL.md)。
